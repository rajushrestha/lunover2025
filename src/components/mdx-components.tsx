import Image from 'next/image';
import Link from 'next/link';

// Define any custom components to be used in MDX
const components = {
  Image,
  Link,
  // Add any other custom components
};

export default components;
